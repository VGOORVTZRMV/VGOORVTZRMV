<%= bootstrap_form_for post, local: true do |form| %>

  <%= form.text_field :title, id: :post_title %>
  <%= form.cktext_area :body, ckeditor: { language: 'uk'}, id: :post_body %>
  <%= form.check_box :published, id: :post_published %>
  <%= form.submit %>

<% end %>
